/**
 * Protocol treasury wallet. In the DFX pipeline the supply not attributable to
 * any user (total supply − users-owned shares) is attributed here as the
 * "protocol-owned shares". In the insurance-fund pipeline the protocol-owned IF
 * slice (totalShares − userShares) per market is attributed here too. See
 * dfx/README.md and insurance-fund/README.md.
 */
export const PROTOCOL_AUTHORITY = "HVoDbY5fWufyposQrdpwsV6w8TkSEi2hS6AjAPz4HRDF";
